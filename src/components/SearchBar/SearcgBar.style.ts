import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    borderWidth: 2,
    borderRadius: 5,
    borderColor: '#aaa',
    padding: 5,
    marginTop: 5,
  },
});
