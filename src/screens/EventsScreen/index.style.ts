import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  view: {
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  list: {
    marginHorizontal: 5,
    width: '100%',
    height: '100%',
  },
});
